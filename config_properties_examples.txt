# 오라클 DB 연결 정보
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:xe
spring.datasource.username=your_user_name
spring.datasource.password=your_user_pwd
spring.datasource.driver-class-name=oracle.jdbc.driver.OracleDriver


# HikariCP Connection Pool Properties
#풀이 연결을 사용할 수 있을 때까지 대기하는 최대 시간(ms)을 지정
spring.datasource.hikari.connection-timeout=30000

#풀의 최대 연결 수 설정
spring.datasource.hikari.maximum-pool-size=20

#연결이 풀에서 유휴 상태로 있을 수 있는 최대 시간(ms)을 지정
spring.datasource.hikari.idle-timeout=600000

#연결 풀의 이름을 지정
spring.datasource.hikari.pool-name=MyHikariCP

#자동 커밋 끄기
spring.datasource.hikari.auto-commit=false


# 이메일 인증
spring.mail.username=your_gmail_account
spring.mail.password=your_gmail_app_pwd


# 파일을 디스크에 쓸 때 까지의 임계값
# 기본값: 0B
# 52,428,000 == 50MB
spring.servlet.multipart.file-size-threshold=52428800
# 개별 파일당 최대 크기
# 기본값: 1MB
# 10,485,760 == 10MB
spring.servlet.multipart.max-file-size=10485760
# HTTP 요청당 최대 크기
# 기본값: 10MB
# 52,428,800 == 50MB
spring.servlet.multipart.max-request-size=52428800

# 채팅방 이미지 저장
my.chatProfile.location=C:/DevlogImg/chatprofile/
my.chatProfile.webpath=/images/chatprofile/

my.chatMessage.location=C:/DevlogImg/chatMessage/
my.chatMessage.webpath=/images/chatMessage/

# IT-NEWS image
my.news.location=C:/DevlogImg/board/ITnews/
my.news.webpath=/images/board/ITnews/

# Blog-image
my.blogWrite.location=C:/DevlogImg/board/blog/blogWriteImg/
my.blogWrite.webpath=/images/board/blog/blogWriteImg/

# 결제: 포트원 키
storeId = store-your_store_id
channelKey = channel-key-your_channel_key
#
secretKey = your_secret_key


# 자유게시판-image
# - Windows11
#my.freeboard.location=C:/DevlogImg/board/freeboard/
# - Ubuntu 22.04
my.freeboard.location=/home/yypark/C:/DevlogImg/board/freeboard/
my.freeboard.webpath=/images/board/freeboard/

